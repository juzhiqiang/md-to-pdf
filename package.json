{
  "name": "md-to-pdf",
  "private": true,
  "version": "1.0.0",
  "description": "🍎 Markdown 预览与 PDF 导出工具 — 基于品牌设计系统",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "check": "turbo run check"
  },
  "packageManager": "pnpm@10.5.2",
  "devDependencies": {
    "@biomejs/biome": "^2.4.4",
    "turbo": "^2.8.10"
  }
}